{
  "name": "amazoncaptcha-ts",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@12.3.4",
  "engines": {
    "node": "^20.19.0 || >=22.12.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm --filter amazoncaptcha run dev",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck"
  }
}
